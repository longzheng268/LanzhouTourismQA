# 快速开始 - 直接命令版

## ⚠️ 重要提示
**所有 `.ps1` 和 `.bat` 脚本已删除，使用以下直接命令。**

---

## 🚀 3步启动

### 第1步：配置API密钥（必须）
编辑文件：`src/main/resources/config.json`
```json
"api_key": "您的MIMO_API_KEY"
```

### 第2步：构建并运行（推荐）
```powershell
.\gradlew.bat clean build run
```

### 第3步：或者仅运行（开发模式）
```powershell
.\gradlew.bat run
```

---

## 📋 常用命令速查

### 构建相关
```powershell
# 清理构建缓存
.\gradlew.bat clean

# 构建项目
.\gradlew.bat build

# 构建并运行
.\gradlew.bat clean build run
```

### 运行相关
```powershell
# 开发模式运行（无需构建）
.\gradlew.bat run

# 运行已构建的JAR
java -jar build/libs/LanzhouTourismQA-1.0.0.jar
```

### 其他命令
```powershell
# 查看依赖
.\gradlew.bat dependencies

# 查看版本
.\gradlew.bat --version

# 清理并重新构建
.\gradlew.bat clean build
```

---

## 🎯 VS Code调试

### 方法1：按F5直接调试
1. 打开VS Code
2. 按 `F5`
3. 选择配置：
   - **Debug LanzhouTourismQA** - 直接调试
   - **Run LanzhouTourismQA (Gradle)** - 先构建再运行

### 方法2：使用任务
1. 按 `Ctrl+Shift+P`
2. 输入 `Tasks: Run Task`
3. 选择：
   - `gradleBuild` - 构建
   - `gradleRun` - 构建并运行
   - `gradleClean` - 清理

---

## 🗄️ 数据库模式

### 启用数据库
编辑 `src/main/resources/config.json`：
```json
{
  "database": {
    "enabled": true,
    "host": "localhost",           // 数据库地址
    "port": 3306,                  // 端口
    "database": "qa_database",     // 数据库名
    "username": "root",            // 用户名
    "password": "your_password"    // 密码
  }
}
```

### 运行
```powershell
.\gradlew.bat run
```

### 自动功能
- ✅ 连接MariaDB
- ✅ 加载 `qa_pairs` 表
- ✅ 保存聊天历史到 `chat_history` 表
- ✅ 数据库失败时自动回退到JSON模式

---

## 🔧 故障排除

### PowerShell权限错误
```powershell
# 临时允许执行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
```

### 找不到Java
下载JDK 17+：https://www.oracle.com/java/technologies/downloads/

### 构建失败
```powershell
# 检查Java版本
java -version

# 清理后重试
.\gradlew.bat clean build
```

### 网络问题
确保可以访问：
- Maven Central（下载依赖）
- api.mimo.com（调用API）

---

## 📁 项目结构

```
LanzhouTourismQA/
├── src/main/kotlin/          # Kotlin源代码
├── src/main/resources/       # 配置和知识库
│   ├── config.json          # 配置（含数据库）
│   └── knowledge_base.json  # 知识库
├── .vscode/                 # VS Code配置
│   ├── launch.json         # F5调试
│   └── tasks.json          # 任务
├── build.gradle.kts         # Gradle配置
└── gradlew.bat             # Gradle包装器
```

---

## ✅ 验证安装

### 检查Java
```powershell
java -version
# 应该显示：openjdk 17.x.x 或更高
```

### 检查Gradle
```powershell
.\gradlew.bat --version
# 应该显示Gradle 8.4
```

### 测试运行
```powershell
.\gradlew.bat run
# 应该启动GUI程序
```

---

## 🎉 完成！

**现在你可以：**
1. ✅ 使用 `.\gradlew.bat run` 运行程序
2. ✅ 按F5在VS Code中调试
3. ✅ 配置数据库模式（可选）
4. ✅ 查看 `使用文档.md` 获取完整说明

**项目状态：** ✅ 完成并可直接使用
**最后更新：** 2026-01-10
